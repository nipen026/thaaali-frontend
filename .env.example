# Base URL of the backend API + Socket.io server (no trailing slash, no /api suffix —
# that's appended by src/api/index.js). Must match whatever PORT the backend is
# actually running on (see backend/.env.example) — there is no hardcoded default here.
VITE_API_URL=http://localhost:4000
